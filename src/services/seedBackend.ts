/*
  seedBackend.ts (stub)
  - Este stub evita falhas no pós-build. 
  - Se precisar semear dados no backend, implemente aqui chamadas à API.
*/

/* eslint-disable no-console */
(async () => {
  try {
    console.log('[seedBackend] Execução de seed do backend ignorada no build do frontend.');
  } catch (err) {
    console.error('[seedBackend] Erro inesperado:', err);
    process.exit(1);
  }
})();
