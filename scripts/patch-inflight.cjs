#!/usr/bin/env node
// placeholder to satisfy postinstall hook during CI/local install
// original project uses this to patch a dependency; here we no-op
console.log('patch-inflight placeholder');
process.exit(0);
